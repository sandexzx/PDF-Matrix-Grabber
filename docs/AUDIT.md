# 🔍 Аудит кода — DMX Grabber

> Дата аудита: 13 февраля 2026  
> Версия проекта: 0.1.0  
> Аудитор: GitHub Copilot (Claude Opus 4.6)

---

## 📋 Общее впечатление

Проект написан **грамотно и профессионально**. Код хорошо структурирован по модулям, каждый файл имеет ясную зону ответственности. Чувствуется, что автор понимает предметную область (GS1 DataMatrix, Честный Знак) и учёл реальные боевые сценарии: обработка 40 000 страниц, прерывание по Ctrl+C, возобновление работы.

**Оценка: 8/10** — крепкий рабочий инструмент с продуманной архитектурой.

---

## ✅ Что сделано хорошо

### 1. Чистая архитектура

Код разделён на 6 независимых модулей с ясными обязанностями:

| Модуль | Ответственность |
|--------|----------------|
| `converter.py` | PDF → изображения |
| `decoder.py` | Изображение → DataMatrix |
| `parser.py` | DataMatrix → структура ЧЗ |
| `exporter.py` | Результаты → CSV |
| `processor.py` | Оркестрация pipeline |
| `cli.py` | Пользовательский интерфейс |

Такое разделение позволяет легко тестировать и заменять каждый слой.

### 2. Выбор библиотек

- **PyMuPDF (fitz)** вместо `pdf2image`/`poppler` — в 3-5 раз быстрее, нет зависимости от внешних бинарников.
- **pylibdmtx** — промышленный стандарт для DataMatrix.
- **Rich** — красивый CLI с прогресс-баром и таблицами.
- **opencv-python-headless** — без лишних GUI-зависимостей.

### 3. Стратегия двухфазного декодирования (`decoder.py`)

Отличный подход: сначала быстрый проход без предобработки (200 мс таймаут), затем — с бинаризацией и увеличенным таймаутом (800 мс). На чистых PDF первая фаза покроет 90%+ кодов, а вторая подхватит проблемные.

### 4. Механизм resume

Инкрементальное сохранение каждые 50 страниц + sidecar-файл прогресса + `--resume` флаг. Для задачи на 40 000 страниц (часы работы) — это **критически важная** фича.

### 5. Корректная обработка Ctrl+C

В параллельном режиме: `SIGINT` игнорируется в дочерних процессах, главный процесс корректно завершает executor и сбрасывает буфер. Это правильный паттерн для `ProcessPoolExecutor`.

### 6. Нормализация GS1 (`parser.py`)

Продуманная обработка реальных кейсов: разные сканеры передают GS-разделитель по-разному (`<GS>`, `\x1d`, `[GS]`, `^\]`). Код нормализует всё к единому виду.

### 7. ROI (Region of Interest)

Возможность задать область интереса на странице + автоматический fallback на полный лист, если код в области не найден. Это экономит время на больших страницах, где DataMatrix занимает малую часть.

### 8. Sanitization для CSV

Корректная обработка управляющих символов при записи в CSV. GS-символ (0x1D) намеренно сохраняется — он является частью кода маркировки.

### 9. Документация

README понятный, с примерами запуска, troubleshooting и описанием структуры. Docstring'и — у всех публичных функций.

---

## ⚠️ Потенциальные проблемы и замечания

### 🔴 Критические (могут привести к потере данных / некорректной работе)

#### 1. Гонка при записи прогресса (`exporter.py`)

```python
header=not progress_path.exists(),
```

Между проверкой `exists()` и фактической записью файл может быть создан другим процессом (хотя в текущей архитектуре запись идёт из одного процесса, стоит учитывать). Более надёжно:

```python
# Всегда проверять наличие заголовка через размер файла
header = not progress_path.exists() or progress_path.stat().st_size == 0
```

#### 2. Дублирование кодов при resume + append (`exporter.py`)

При использовании `--resume` файл `results.csv` дополняется (`mode="a"`). Но если пользователь запустит скрипт с `--resume` **дважды подряд без прерывания**, прогресс-файл не будет очищен — коды продублируются.

**Рекомендация**: при запуске без прерывания (полная обработка) очищать progress-файл. Или при `export_to_csv` (первая запись) перезаписывать основной CSV.

#### 3. Утечка ресурсов при исключении в `render_page` (`converter.py`)

Если между `fitz.open()` и завершением `with`-блока произойдёт ошибка, `Image.frombytes` вернёт объект, но `pix` и `page` не будут явно освобождены. На 40 000 страниц это может не проявиться, но при использовании в длительных процессах стоит использовать `try/finally` или явный `del`.

---

### 🟡 Важные (влияют на надёжность / поддерживаемость)

#### 4. Все задачи сразу в executor (`processor.py`)

```python
futures = {
    executor.submit(...): (...)
    for pdf_path, page_num in task_queue
}
```

При 40 000 страниц **все** задачи отправляются в очередь сразу. Это создаёт 40 000 объектов `Future` в памяти. При workers=4 реально работают только 4, а остальные 39 996 висят в очереди.

**Рекомендация**: отправлять задачи батчами или использовать `concurrent.futures.as_completed` с ограниченной очередью:

```python
# Вариант: итерировать по chunk'ам
CHUNK = workers * 4
for i in range(0, len(task_queue), CHUNK):
    chunk = task_queue[i:i+CHUNK]
    futures = {executor.submit(...): key for ...}
    for future in as_completed(futures):
        ...
```

#### 5. Файл PDF открывается заново на каждую страницу (`processor.py` → `converter.py`)

Функция `render_page` вызывает `fitz.open()` / `close()` на каждую страницу. При 40 000 страниц одного файла — это 40 000 открытий. Хотя `fitz.open()` достаточно быстр, `render_pages_batch` (которая уже реализована!) не используется.

**Рекомендация**: для последовательного режима использовать `render_pages_batch`. Для параллельного — передавать батч страниц одного файла в один процесс.

#### 6. `ROI_NORM` — модульная переменная (`converter.py`)

```python
ROI_NORM: tuple[float, float, float, float] | None = None
```

ROI задаётся как глобальная константа в модуле. Это значит:
- Нет возможности задать ROI через CLI.
- При импорте в тестах значение разделяется между тестами.
- В параллельном режиме значение корректно копируется в дочерние процессы (т.к. `fork`), но на Windows (`spawn`) может вести себя иначе.

**Рекомендация**: передавать ROI как параметр функции или через конфиг.

#### 7. Backward compatibility заглушки (`exporter.py`)

```python
export_to_excel = export_to_csv
append_to_excel = append_to_csv
```

Оставлять «мёртвые» alias'ы — антипаттерн. Если внешний код на них завязан, стоит добавить `warnings.warn` с `DeprecationWarning`. Если нет — удалить.

#### 8. Тип поля `status` в `ProcessingResult` (`models.py`)

```python
status: str = Status.OK
```

Тип аннотирован как `str`, но дефолт — `Status.OK` (enum). А в `processor.py`:

```python
status=f"{Status.ERROR}: {e}",
```

Статус записывается как произвольная строка. Из-за этого в `exporter.py` пришлось делать громоздкую проверку:

```python
def _is_ok_status(status: object) -> bool:
    if status == "OK":
        return True
    if hasattr(status, "value") and getattr(status, "value") == "OK":
        return True
    return str(status) in {"OK", "Status.OK"}
```

**Рекомендация**: определиться — либо `status` всегда `Status` (enum), а ошибку хранить в отдельном поле `error_message: str | None`, либо всегда `str`. Текущий подход «то enum, то строка» создаёт хрупкость.

#### 9. Нет валидации DPI (`cli.py`)

Пользователь может задать `--dpi 0` или `--dpi -100`. Стоит добавить проверку:

```python
if args.dpi < 72:
    console.print("[red]DPI должен быть не менее 72[/]")
    return 1
```

#### 10. Нет валидации workers

Аналогично: `--workers 0` или `--workers -5` не проверяются.

---

### 🟢 Мелкие замечания (качество кода)

#### 11. Жёстко зашитые таймауты (`decoder.py`)

```python
FIRST_PASS_TIMEOUT_MS = 200
SECOND_PASS_TIMEOUT_MS = 800
```

Для проекта «сделал и отдал» — нормально. Но если код будут переиспользовать на других данных (разный размер матриц, разное качество), стоит вынести в конфигурацию.

#### 12. Поиск `"01"` в произвольной позиции (`parser.py`)

```python
if "01" in code:
    idx = code.index("01")
```

Подстрока `"01"` может встретиться в серийном номере или другой части кода. Корректнее проверять, что код **начинается** с `"01"`:

```python
if code.startswith("01"):
    idx = 0
```

Или проверять, что `idx == 0`, т.к. AI 01 (GTIN) всегда идёт первым в коде DataMatrix для Честного Знака.

#### 13. `openpyxl` в зависимостях, но не используется

В `requirements.txt` и `pyproject.toml` указан `openpyxl`, но экспорт идёт в CSV. Единственное использование — fallback в `load_progress` для «старого формата xlsx». Если xlsx больше не генерируется, зависимость можно убрать.

#### 14. `setuptools` в зависимостях

`setuptools>=70.0.0` в `dependencies` — обычно это build-зависимость, а не runtime. Если она там для `pkg_resources` — лучше заменить на `importlib.metadata`.

#### 15. Entry point ссылается на `__main__:main`, но `main` определён в `cli.py`

```toml
[project.scripts]
dmx-grabber = "dmx_grabber.__main__:main"
```

Но в `__main__.py` нет функции `main` на верхнем уровне — есть только `from .cli import main` и `sys.exit(main())`. Технически работает, но лучше явно:

```toml
dmx-grabber = "dmx_grabber.cli:main"
```

#### 16. Нет тестов

Для «сделал и отдал» — допустимо. Но если проект будет развиваться, стоит добавить хотя бы:
- Unit-тест на `parser.py` (парсинг различных форматов кода).
- Unit-тест на `decoder.py` (можно с фикстурой — картинкой с DataMatrix).
- Integration-тест на `exporter.py` (запись/чтение CSV + resume).

#### 17. Нет `.gitignore`

В репозитории нет `.gitignore`. Стоит добавить:

```
__pycache__/
*.pyc
.venv/
output/
data/input/*.pdf
*.egg-info/
dist/
build/
```

#### 18. Нет логирования

Все сообщения идут через `rich.console.print`. Для отладки проблем в продакшене (особенно при 40 000 страниц) полезно иметь `logging` с записью в файл — с уровнями DEBUG/WARNING/ERROR.

---

## 🏗️ Архитектурные рекомендации (если проект будет развиваться)

### Batch-обработка страниц одного PDF

Сейчас каждая страница — отдельная задача. Эффективнее группировать: один воркер берёт батч из 10-20 страниц одного PDF, открывает файл один раз и обрабатывает все страницы за одно открытие. Функция `render_pages_batch` уже реализована, но не используется.

### Конфигурационный файл

Вместо хардкода `ROI_NORM`, таймаутов, `SAVE_EVERY`, `MAX_CODES_PER_PAGE` — использовать TOML/YAML конфиг или расширить CLI-параметры.

### Типизация

Проект уже использует `Python 3.10+` синтаксис (`X | Y`). Стоит добавить `py.typed` маркер и запустить `mypy` / `pyright` для полной проверки типов. Основная проблема — поле `status` (см. п.8 выше).

---

## 📊 Сводка

| Категория | Оценка | Комментарий |
|-----------|--------|-------------|
| Архитектура | ⭐⭐⭐⭐⭐ | Чистое разделение на модули |
| Надёжность | ⭐⭐⭐⭐ | Resume + Ctrl+C — отлично; есть edge cases |
| Производительность | ⭐⭐⭐⭐ | PyMuPDF + двухфазный декодинг; batch не используется |
| Код | ⭐⭐⭐⭐ | Читаемый, документированный; мелкие шероховатости с типами |
| Тестируемость | ⭐⭐ | Нет тестов |
| UX | ⭐⭐⭐⭐⭐ | Rich-интерфейс, понятные сообщения, README |
| Безопасность | ⭐⭐⭐⭐ | Sanitization для CSV, корректная обработка unicode |

---

## ✏️ Приоритеты для доработки (если потребуется)

1. **🔴 Исправить смешанный тип `status`** — enum vs строка (п.8)
2. **🔴 Проверить сценарий двойного resume** — дублирование кодов (п.2)
3. **🟡 Добавить валидацию CLI-параметров** — DPI, workers (п.9, 10)
4. **🟡 Использовать batch-рендеринг** — уже написан, не подключен (п.5)
5. **🟡 Исправить поиск AI 01** — `startswith` вместо `in` (п.12)
6. **🟢 Убрать лишние зависимости** — openpyxl, setuptools (п.13, 14)
7. **🟢 Добавить `.gitignore`** (п.17)
8. **🟢 Добавить тесты** для парсера и экспортера (п.16)
