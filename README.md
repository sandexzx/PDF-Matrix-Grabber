# 📦 DMX Grabber

Массовое считывание DataMatrix кодов (Честный Знак) из PDF файлов с экспортом в Excel.

---

## ⚡ Быстрый старт

### 1. Системные зависимости

```bash
# Ubuntu / Debian
sudo apt install libdmtx0b

# macOS
brew install libdmtx

# Arch Linux
sudo pacman -S libdmtx
```

### 2. Установка Python-пакетов

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 3. Запуск

Поместите PDF-файлы в `data/input/` и запустите:

```bash
python main.py
```

Результат появится в `output/results_<timestamp>.xlsx`.

---

## 🛠️ Параметры запуска

```
python main.py [опции]

Опции:
  -i, --input PATH     Папка с PDF файлами (по умолчанию: data/input)
  -o, --output PATH    Путь для Excel-результата (по умолчанию: output/results.xlsx)
  -w, --workers N      Кол-во параллельных процессов (по умолчанию: 1)
  --dpi N              Разрешение рендеринга PDF (по умолчанию: 300)
  --resume             Продолжить с места прерывания
  --no-parse           Не парсить коды как Честный Знак
  --limit N            Макс. страниц для обработки (для тестирования)
  -v, --version        Показать версию
  -h, --help           Показать справку
```

### Примеры

```bash
# Быстрая обработка — 4 процесса
python main.py -w 4

# Максимальная скорость на 8-ядерной машине
python main.py -w 8

# Прервалось? Продолжаем с того же места
python main.py -w 4 --resume

# Обработать конкретную папку
python main.py -i /path/to/pdfs -o результат.xlsx -w 4

# Увеличить качество распознавания
python main.py --dpi 600

# Тест на первых 50 страницах
python main.py --limit 50
```

---

## ⚡ Производительность

| Режим | Скорость | 40000 страниц |
|-------|----------|---------------|
| `-w 1` (последовательно) | ~1 сек/стр | ~11 часов |
| `-w 4` (4 процесса) | ~0.26 сек/стр | ~2.9 часа |
| `-w 8` (8 процессов) | ~0.15 сек/стр* | ~1.7 часа |

*\* зависит от количества ядер CPU*

### Защита от прерываний

- Результаты сохраняются в Excel каждые 50 страниц
- `Ctrl+C` — корректное завершение с сохранением прогресса
- `--resume` — продолжение с места остановки (пропускает уже обработанные страницы)
  по служебному файлу `*.xlsx.progress.csv`

---

## 📁 Структура проекта

```
PDF Matrix Grabber/
├── src/dmx_grabber/        # Основной пакет
│   ├── cli.py              # CLI-интерфейс (Rich)
│   ├── converter.py        # PDF → изображения
│   ├── decoder.py          # Декодирование DataMatrix
│   ├── exporter.py         # Экспорт в Excel
│   ├── models.py           # Модели данных
│   ├── parser.py           # Парсинг кодов Честного Знака
│   └── processor.py        # Оркестрация pipeline
├── data/input/             # PDF файлы для обработки
├── output/                 # Результаты (Excel)
├── docs/                   # Документация
├── main.py                 # Точка входа
├── requirements.txt
└── pyproject.toml
```

---

## 📊 Что в результате

Excel-файл содержит только коды DataMatrix:

- один столбец
- без заголовка
- только найденные коды (без пустых/служебных строк)

---

## 🔧 Troubleshooting

**Коды не распознаются:**
- Увеличьте DPI: `--dpi 600`
- Проверьте, что PDF содержит DataMatrix, а не QR-код
- Убедитесь, что `libdmtx` установлена в системе

**Медленная работа:**
- Увеличьте число воркеров: `-w 8`
- Уменьшите DPI до 200 (может снизить точность)
- По умолчанию включён быстрый режим для кейса `1 код/страница`
  (`pylibdmtx max_count=1` + короткие таймауты).
- Для шаблонов с фиксированной зоной кода можно задать `ROI_NORM`
  в `src/dmx_grabber/converter.py` и ускорить обработку больших страниц.
  При промахе в ROI система автоматически делает fallback на полный лист.

Новая рекомендуемая область:

ROI_NORM = (0.1525, 0.2275, 0.8547, 0.6297)
