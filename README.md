# 📦 DMX Grabber

Массовое считывание DataMatrix кодов (Честный Знак) из PDF файлов с экспортом в Excel.

---

## ⚡ Быстрый старт

### 1. Системные зависимости

```bash
# Ubuntu / Debian
sudo apt install libdmtx0b poppler-utils

# macOS
brew install libdmtx poppler

# Arch Linux
sudo pacman -S libdmtx poppler
```

### 2. Установка Python-пакетов

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 3. Запуск

Поместите PDF-файлы в `data/input/` и запустите:

```bash
python main.py
```

Результат появится в `output/results_<timestamp>.xlsx`.

---

## 🛠️ Параметры запуска

```
python main.py [опции]

Опции:
  -i, --input PATH     Папка с PDF файлами (по умолчанию: data/input)
  -o, --output PATH    Путь для Excel-результата (по умолчанию: output/results_<timestamp>.xlsx)
  --dpi N              Разрешение рендеринга PDF (по умолчанию: 300)
  --no-parse           Не парсить коды как Честный Знак
  -v, --version        Показать версию
  -h, --help           Показать справку
```

### Примеры

```bash
# Обработать конкретную папку
python main.py -i /path/to/pdfs -o результат.xlsx

# Увеличить качество распознавания
python main.py --dpi 600

# Только сырые коды без парсинга ЧЗ
python main.py --no-parse
```

---

## 📁 Структура проекта

```
PDF Matrix Grabber/
├── src/dmx_grabber/        # Основной пакет
│   ├── cli.py              # CLI-интерфейс (Rich)
│   ├── converter.py        # PDF → изображения
│   ├── decoder.py          # Декодирование DataMatrix
│   ├── exporter.py         # Экспорт в Excel
│   ├── models.py           # Модели данных
│   ├── parser.py           # Парсинг кодов Честного Знака
│   └── processor.py        # Оркестрация pipeline
├── data/input/             # PDF файлы для обработки
├── output/                 # Результаты (Excel)
├── docs/                   # Документация
├── main.py                 # Точка входа
├── requirements.txt
└── pyproject.toml
```

---

## 📊 Что в результате

Excel-файл со столбцами:

| Столбец | Описание |
|---------|----------|
| Файл | Имя PDF-файла |
| Страница | Номер страницы |
| DataMatrix (raw) | Сырые данные из кода |
| GTIN | Глобальный номер товара (14 цифр) |
| Серийный номер | Серийный номер единицы |
| Ключ проверки | 4-символьный ключ |
| Криптохвост | Криптографическая подпись |
| Статус | OK / НЕ НАЙДЕН / ОШИБКА |

---

## 🔧 Troubleshooting

**Коды не распознаются:**
- Увеличьте DPI: `--dpi 600`
- Проверьте, что PDF содержит DataMatrix, а не QR-код
- Убедитесь, что `libdmtx` установлена в системе

**Ошибка `pdf2image`:**
- Установите `poppler-utils`: `sudo apt install poppler-utils`

**Медленная работа:**
- Уменьшите DPI до 200 (может снизить точность)
- Параллелизация будет добавлена в следующей версии
