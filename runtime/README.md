# Runtime files

This folder contains optional local runtime files for Windows users.

- `python-installer/` - offline Python installer (`python-*.exe`).
- `libdmtx/` - DataMatrix DLL files (`libdmtx-64.dll`).

The launcher `run_windows.cmd` automatically uses these folders when available.
